# ✅ File: .github/workflows/SlowContro.yml
# WORKFLOW ĐIỀU KHIỂN (PHIÊN BẢN SIÊU ỔN ĐỊNH - DÙNG GREP + AWK)

name: 🔄 Điều Khiển Vòng Lặp Slow (Bản Siêu Ổn Định)

on:
  workflow_dispatch:
    inputs:
      target_url:
        description: 'URL mục tiêu cho các lần test'
        required: true
        default: 'https://certapple.com'
      wait_minutes:
        description: 'Số phút chờ trước khi khởi động lại'
        required: true
        default: '6'
      child_runs:
        description: 'Số action con (Slow.yml) được khởi chạy trong mỗi chu kỳ'
        required: true
        default: '5'

jobs:
  loop_controller:
    name: 🔄 Vòng Lặp Điều Khiển
    runs-on: ubuntu-latest
    
    permissions:
      actions: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Bắt đầu vòng lặp vô tận
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_URL: ${{ github.event.inputs.target_url }}
          WAIT_MINUTES: ${{ github.event.inputs.wait_minutes }}
          CHILD_RUNS: ${{ github.event.inputs.child_runs }}
        run: |
          set -e
          
          WAIT_SECONDS=$(($WAIT_MINUTES * 60))

          echo "Bắt đầu vòng lặp điều khiển..."
          echo "URL mục tiêu: $TARGET_URL"
          echo "Số lần chạy con mỗi vòng: $CHILD_RUNS"
          echo "Thời gian chờ mỗi vòng: $WAIT_MINUTES phút ($WAIT_SECONDS giây)"

          RUN_IDS_TO_CANCEL=""

          while true; do
            echo "-----------------------------------------------------"
            
            # Dừng các action từ vòng lặp TRƯỚC (nếu có)
            if [ -n "$RUN_IDS_TO_CANCEL" ]; then
              echo "🛑 Bắt đầu dừng $CHILD_RUNS actions từ vòng lặp trước..."
              for id in $RUN_IDS_TO_CANCEL; do
                echo "-> Đang gửi lệnh hủy cho run ID: $id"
                gh run cancel $id || echo "Lỗi khi hủy run ID $id (có thể nó đã kết thúc hoặc không tồn tại)."
              done
              echo "✅ Đã gửi lệnh hủy cho các action cũ."
            else
              echo "ℹ️ Vòng lặp đầu tiên, không có action cũ để dừng."
            fi

            # THAY ĐỔI LỚN NHẤT: DÙNG PHƯƠNG PHÁP 2 BƯỚC (GREP + AWK)
            echo "🚀 Bắt đầu khởi chạy $CHILD_RUNS actions mới và lấy ID..."
            
            NEW_RUN_IDS=""
            
            for i in $(seq 1 $CHILD_RUNS); do
              # Chạy workflow và chụp lại TOÀN BỘ output
              RUN_OUTPUT=$(gh workflow run Slow.yml --ref ${{ github.ref }} -f target_url="$TARGET_URL")
              
              # Bước 1: Dùng `grep -o` để lọc ra chỉ chuỗi URL từ output
              RUN_URL=$(echo "$RUN_OUTPUT" | grep -o 'https://github.com/.*/actions/runs/[0-9]*')
              
              if [ -n "$RUN_URL" ]; then
                # Bước 2: Sau khi đã có URL sạch, dùng `awk` để lấy ID
                RUN_ID=$(echo "$RUN_URL" | awk -F'/' '{print $NF}')
                echo "-> Đã kích hoạt lần chạy $i với ID: $RUN_ID"
                NEW_RUN_IDS="$NEW_RUN_IDS $RUN_ID"
              else
                echo "⚠️ Lỗi: Không thể trích xuất URL từ output. Full output từ gh:"
                echo "$RUN_OUTPUT"
              fi
            done
            
            # Gán danh sách ID mới vào biến sẽ được dùng để hủy ở vòng lặp sau
            RUN_IDS_TO_CANCEL=$(echo "$NEW_RUN_IDS" | xargs) # xargs để xóa khoảng trắng thừa
            
            if [ -z "$RUN_IDS_TO_CANCEL" ]; then
                echo "‼️ Lỗi nghiêm trọng: Không thể kích hoạt và lấy ID của bất kỳ action con nào. Sẽ thử lại sau $WAIT_MINUTES phút."
            else
                echo "✅ Đã lấy được các ID để hủy ở vòng lặp sau: $RUN_IDS_TO_CANCEL"
            fi

            # Chờ đợi để các action mới chạy
            echo "⏳ Chờ $WAIT_SECONDS giây ($WAIT_MINUTES phút)..."
            sleep $WAIT_SECONDS
            
            echo "🔄 Hoàn tất chu kỳ, chuẩn bị cho vòng lặp tiếp theo..."
          done
